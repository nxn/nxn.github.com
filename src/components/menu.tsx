import React from "react";
import styled from "@emotion/styled";

export const HeaderMenu = styled('ul')(({theme: { main: theme }}) => ({

}));

export const ResumeHeaderMenu = styled('ul')(({theme: { resume: theme }}) => ({

}));
